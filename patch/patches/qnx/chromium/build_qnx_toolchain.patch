diff --git build/config/BUILD.gn build/config/BUILD.gn
index 96085b41f084da34e7e405dc8cbabdaf2161c300..92577a21d502a1dd9f58fbbc3b452dc8374a0a62 100644
--- build/config/BUILD.gn
+++ build/config/BUILD.gn
@@ -209,6 +209,15 @@ config("default_libs") {
     # Targets should choose to explicitly link frameworks they require. Since
     # linking can have run-time side effects, nothing should be listed here.
     libs = []
+  } else if (is_qnx) {
+    libs = [
+      "c++",
+      "m",
+      "regex",
+      "socket",
+      "fsnotify",
+      "eventfd",
+    ]
   } else if (is_linux || is_chromeos) {
     libs = [
       "dl",
diff --git build/config/BUILDCONFIG.gn build/config/BUILDCONFIG.gn
index 4d1ac59b9b7dc05a20a3bb523b17706a00761cde..964371eae8b27488f0a95ba54454339acde8403c 100644
--- build/config/BUILDCONFIG.gn
+++ build/config/BUILDCONFIG.gn
@@ -276,6 +276,9 @@ if (target_os == "android") {
   _default_toolchain = "//build/toolchain/aix:$target_cpu"
 } else if (target_os == "zos") {
   _default_toolchain = "//build/toolchain/zos:$target_cpu"
+} else if (target_os == "qnx") {
+  assert(host_os == "linux", "QNX builds are only supported on Linux.")
+  _default_toolchain = "//build/toolchain/qnx:clang_$target_cpu"
 } else if (target_os == "emscripten") {
   # Because it's too hard to remove all targets from //BUILD.gn that do not work with it.
   assert(
@@ -312,6 +315,7 @@ if (custom_toolchain != "") {
 # current_os value directly.
 
 is_android = current_os == "android"
+is_qnx = current_os == "qnx"
 is_chromeos = current_os == "chromeos"
 is_fuchsia = current_os == "fuchsia"
 is_ios = current_os == "ios"
diff --git build/config/clang/BUILD.gn build/config/clang/BUILD.gn
index e05032c0c66f1a2801971fbe9ee8a85f43bda2f4..7b34df06343b03e7a6b4faa858364ddba5a7a093 100644
--- build/config/clang/BUILD.gn
+++ build/config/clang/BUILD.gn
@@ -156,6 +156,56 @@ group("llvm-symbolizer_data") {
   }
 }
 
+if (is_qnx) {
+  _qnx_host = getenv("QNX_HOST")
+  _qnx_target = getenv("QNX_TARGET")
+  assert(_qnx_host != "", "QNX_HOST must be set for QNX compiler-rt builtins")
+  assert(_qnx_target != "",
+         "QNX_TARGET must be set for QNX compiler-rt builtins")
+
+  if (current_cpu == "x64") {
+    _qnx_compiler_rt_target = "x86_64-unknown-nto"
+    _qnx_ar = "${_qnx_host}/usr/bin/ntox86_64-ar"
+    _qnx_ranlib = "${_qnx_host}/usr/bin/ntox86_64-ranlib"
+  } else if (current_cpu == "arm64") {
+    _qnx_compiler_rt_target = "aarch64-unknown-nto"
+    _qnx_ar = "${_qnx_host}/usr/bin/ntoaarch64-ar"
+    _qnx_ranlib = "${_qnx_host}/usr/bin/ntoaarch64-ranlib"
+  } else {
+    assert(false, "Unhandled QNX cpu type: $current_cpu")
+  }
+
+  _qnx_compiler_rt_output =
+      "$root_out_dir/qnx_clang_rt/${_qnx_compiler_rt_target}/libclang_rt.builtins.a"
+
+  action("qnx_compiler_builtins") {
+    script = "//build/config/qnx/build_compiler_rt_builtins.py"
+    depfile = "$target_gen_dir/qnx_compiler_builtins.d"
+    outputs = [ _qnx_compiler_rt_output ]
+    args = [
+      "--clang",
+      rebase_path("$clang_base_path/bin/clang", root_build_dir),
+      "--ar",
+      _qnx_ar,
+      "--ranlib",
+      _qnx_ranlib,
+      "--sysroot",
+      _qnx_target,
+      "--target",
+      _qnx_compiler_rt_target,
+      "--arch",
+      current_cpu,
+      "--source-root",
+      rebase_path("//third_party/compiler-rt/src/lib/builtins", root_build_dir),
+      "--output",
+      rebase_path(_qnx_compiler_rt_output, root_build_dir),
+      "--depfile",
+      rebase_path(depfile, root_build_dir),
+    ]
+    inputs = [ script ]
+  }
+}
+
 template("clang_lib") {
   if (!defined(invoker.libname) || is_wasm) {
     not_needed(invoker, "*")
@@ -168,6 +218,7 @@ template("clang_lib") {
       _prefix = "lib"
       _suffix = ""
       _ext = "a"
+      _clang_lib_dir = "$clang_base_path/lib/clang/$clang_version/lib"
 
       if (is_win) {
         _dir = "windows"
@@ -184,6 +235,15 @@ template("clang_lib") {
         }
       } else if (is_apple) {
         _dir = "darwin"
+      } else if (is_qnx) {
+        if (current_cpu == "x64") {
+          _dir = "x86_64-unknown-nto"
+        } else if (current_cpu == "arm64") {
+          _dir = "aarch64-unknown-nto"
+        } else {
+          assert(false)  # Unhandled cpu type
+        }
+        _clang_lib_dir = "$root_out_dir/qnx_clang_rt"
       } else if (is_linux || is_chromeos) {
         if (current_cpu == "x64") {
           _dir = "x86_64-unknown-linux-gnu"
@@ -231,7 +291,6 @@ template("clang_lib") {
         assert(false)  # Unhandled target platform
       }
 
-      _clang_lib_dir = "$clang_base_path/lib/clang/$clang_version/lib"
       _lib_file = "${_prefix}clang_rt.${_libname}${_suffix}.${_ext}"
       libs = [ "$_clang_lib_dir/$_dir/$_lib_file" ]
     }
diff --git build/config/compiler/BUILD.gn build/config/compiler/BUILD.gn
index ec3d374d13674fde452beb555a6cf43b9a4dbd3a..63bcc2755a6621a18547b49004ae804bb5293110 100644
--- build/config/compiler/BUILD.gn
+++ build/config/compiler/BUILD.gn
@@ -1059,6 +1059,13 @@ config("linker") {
         current_os != "zos") {
       ldflags += [ "-Wl,--fatal-warnings" ]
     }
+    if (is_qnx) {
+      # QNX SDP startup objects (for example crtn.o) do not carry
+      # .note.GNU-stack, which makes GNU ld warn about an implied executable
+      # stack. Keep fatal linker warnings enabled, but suppress only this
+      # compatibility warning from the QNX-provided startup objects.
+      ldflags += [ "-Wl,--no-warn-execstack" ]
+    }
     if (fatal_linker_warnings && is_apple) {
       ldflags += [ "-Wl,-fatal_warnings" ]
     }
@@ -1313,7 +1320,11 @@ config("compiler_cpu_abi") {
     # CPU architecture. We may or may not be doing a cross compile now, so for
     # simplicity we always explicitly set the architecture.
     if (current_cpu == "x64") {
-      if (is_clang && !is_android && !is_fuchsia && !is_chromeos_device) {
+      if (is_qnx) {
+        # QNX target selection is handled by //build/toolchain/qnx. Do not pass
+        # Linux clang or generic -m64 flags to qcc when linking.
+      } else if (is_clang && !is_android && !is_fuchsia &&
+                 !is_chromeos_device) {
         cflags += [ "--target=x86_64-unknown-linux-gnu" ]
         ldflags += [ "--target=x86_64-unknown-linux-gnu" ]
       } else {
@@ -1348,7 +1359,8 @@ config("compiler_cpu_abi") {
         "-mfloat-abi=$arm_float_abi",
       ]
     } else if (current_cpu == "arm64") {
-      if (is_clang && !is_android && !is_fuchsia && !is_chromeos_device) {
+      if (is_clang && !is_android && !is_fuchsia && !is_chromeos_device &&
+          !is_qnx) {
         cflags += [ "--target=aarch64-linux-gnu" ]
         ldflags += [ "--target=aarch64-linux-gnu" ]
       }
@@ -1739,7 +1751,7 @@ config("compiler_deterministic") {
     # directly there, not through the compiler driver.
     # We don't link on RBE, so this change is just for cleaner
     # internal linker invocations, for people who work on the build.
-    if (!is_win) {
+    if (!is_win && !is_qnx) {
       ldflags += [ "-no-canonical-prefixes" ]
     }
   }
@@ -2399,7 +2411,7 @@ config("chromium_code") {
       "__STDC_FORMAT_MACROS",
     ]
 
-    if (!is_debug && !using_sanitizer && current_cpu != "s390x" &&
+    if (!is_debug && !using_sanitizer && !is_qnx && current_cpu != "s390x" &&
         current_cpu != "ppc64" && current_cpu != "mips" &&
         current_cpu != "mips64" && current_cpu != "riscv64" &&
         current_cpu != "loong64") {
@@ -2419,7 +2431,7 @@ config("chromium_code") {
       # with the sysroot where support is guaranteed. Clang is also required since
       # the system gcc is often patched to add _FORTIFY_SOURCE=2 which would conflict
       # with our definition.
-      if (use_sysroot && is_clang) {
+      if (use_sysroot && is_clang && !is_qnx) {
         fortify_level = "3"
       }
       defines += [ "_FORTIFY_SOURCE=" + fortify_level ]
@@ -2596,7 +2608,8 @@ config("export_dynamic") {
 # 2. Remove the thin_archive config, so that the .a file actually contains all
 #    .o files, instead of just references to .o files in the build directoy
 config("thin_archive") {
-  if ((is_apple && use_lld) || (is_linux && !is_clang) || current_os == "aix") {
+  if ((is_apple && use_lld) || (is_linux && !is_clang) ||
+      current_os == "aix" || is_qnx) {
     # The macOS and iOS linker ld64.ldd doesn't support thin archive without
     # symbol table, gcc on linux also throws the error `archive has no index`.
     # AIX does support -s option, not -S option.
diff --git build/config/compiler/compiler.gni build/config/compiler/compiler.gni
index beb5213714e1ab6260ed735099b0bcac90cdab66..44d0bd5c6609d652a924b5af8c3b4963cb8e8e92 100644
--- build/config/compiler/compiler.gni
+++ build/config/compiler/compiler.gni
@@ -56,6 +56,11 @@ declare_args() {
       !is_high_end_android && (is_android || is_castos || is_fuchsia)
 }
 
+# QNX uses QCC as the linker driver, which does not support LLD.
+if (current_os == "qnx") {
+  use_lld = false
+}
+
 if (is_mac && use_lld) {
   declare_args() {
     # Enables the `--read-workers` flag for LLD.
diff --git build/config/posix/BUILD.gn build/config/posix/BUILD.gn
index 9638527df0271a27fad3fe29a11f814e3b982ee5..25c75003b8d3b0a463cfc7b6029ccb8a46cba6f8 100644
--- build/config/posix/BUILD.gn
+++ build/config/posix/BUILD.gn
@@ -37,7 +37,9 @@ config("runtime_library") {
       defines += [ "CR_SYSROOT_KEY=$cr_sysroot_key" ]
     }
     asmflags += sysroot_flags
-    ldflags += sysroot_flags
+    if (current_os != "qnx") {
+      ldflags += sysroot_flags
+    }
 
     # When use_custom_libcxx=true, some -isystem flags get passed to
     # cflags_cc to set up libc++ include paths.  We want to make sure
diff --git build/config/rust.gni build/config/rust.gni
index a55e7c58b9c33409e1cae96bbd81b6a286a93ab3..eadbd477d1f5d65e81661c57f43948ac4ca17dfe 100644
--- build/config/rust.gni
+++ build/config/rust.gni
@@ -211,7 +211,15 @@ if (enable_rust) {
 # (see '*_toolchain_supports_platform above') to enable experimentation with
 # other toolchains.
 rust_abi_target = ""
-if (is_linux || is_chromeos) {
+if (is_qnx) {
+  if (current_cpu == "arm64") {
+    rust_abi_target = "aarch64-unknown-nto-qnx800"
+  } else if (current_cpu == "x64") {
+    rust_abi_target = "x86_64-pc-nto-qnx800"
+  } else {
+    assert(false, "Architecture not supported")
+  }
+} else if (is_linux || is_chromeos) {
   if (current_cpu == "arm64") {
     rust_abi_target = "aarch64-unknown-linux-gnu"
   } else if (current_cpu == "x86") {
diff --git build/config/sysroot.gni build/config/sysroot.gni
index e7aaa18479d47958d6407f4d22bbe7ea939e1f92..caa1b470108b6586d40ced27ce19b21f106d5eba 100644
--- build/config/sysroot.gni
+++ build/config/sysroot.gni
@@ -29,6 +29,8 @@ if (sysroot == "") {
 
     # Android uses unified headers, and thus a single compile time sysroot
     sysroot = "$android_toolchain_root/sysroot"
+  } else if (is_qnx) {
+    sysroot = getenv("QNX_TARGET")
   } else if ((is_linux || is_chromeos) && use_sysroot) {
     # By default build against a sysroot image downloaded from Cloud Storage
     # during gclient runhooks.
diff --git build/rust/known-target-triples.txt build/rust/known-target-triples.txt
index fc4780bf65bb3df4c76555e72dce2110bf0bde0a..2c84c63f1716dee70d18991cbd29f2667c5913e7 100644
--- build/rust/known-target-triples.txt
+++ build/rust/known-target-triples.txt
@@ -41,5 +41,7 @@ x86_64-linux-android
 x86_64-pc-windows-msvc
 x86_64-unknown-fuchsia
 x86_64-unknown-linux-gnu
+x86_64-pc-nto-qnx800
+aarch64-unknown-nto-qnx800
 powerpc64le-unknown-linux-gnu
 s390x-unknown-linux-gnu
diff --git build/toolchain/gcc_toolchain.gni build/toolchain/gcc_toolchain.gni
index a22678bb17ff94386dec983cab699f1eee332d19..7b33e784ca8393e2560a2e84fd3060fb62c27f41 100644
--- build/toolchain/gcc_toolchain.gni
+++ build/toolchain/gcc_toolchain.gni
@@ -415,7 +415,26 @@ template("single_gcc_toolchain") {
         # -soname flag is not available on aix ld
         soname_flag = "-Wl,-soname=\"$soname\""
       }
-      link_command = "$ld -shared $soname_flag {{ldflags}}${extra_ldflags} -o \"$unstripped_sofile\" @\"$rspfile\" {{rlibs}}"
+      start_group_flag = ""
+      end_group_flag = ""
+      if (current_os != "aix") {
+        start_group_flag = "-Wl,--start-group"
+        end_group_flag = "-Wl,--end-group "
+      }
+      if (current_os == "qnx") {
+        # QNX's qcc/ld behaves like a left-to-right, single-pass linker here.
+        # Rust rlibs are alpha-sorted, and native -lfoo dependencies are also
+        # emitted from the rspfile, so the command line order matters: if a
+        # native library is seen before the Rust crate that needs it, the
+        # linker may not revisit it. Wrap {{rlibs}} and {{libs}} in
+        # --start-group/--end-group so the linker can revisit them to resolve
+        # circular/forward Rust crate references.
+        link_command =
+            "$ld -shared $soname_flag {{ldflags}}${extra_ldflags} -o \"$unstripped_sofile\" @\"$rspfile\" $start_group_flag {{rlibs}} {{libs}} $end_group_flag"
+      } else {
+        link_command =
+            "$ld -shared $soname_flag {{ldflags}}${extra_ldflags} -o \"$unstripped_sofile\" @\"$rspfile\" {{rlibs}}"
+      }
 
       # Generate a map file to be used for binary size analysis.
       # Map file adds ~10% to the link time on a z620.
@@ -447,7 +466,9 @@ template("single_gcc_toolchain") {
       }
       command = "\"$python_path\" \"$solink_wrapper\" --readelf=\"$readelf\" --nm=\"$nm\" $strip_switch$dwp_switch --sofile=\"$unstripped_sofile\" --tocfile=\"$tocfile\"$map_switch --output=\"$sofile\" -- $link_command $solink_extra_flags"
 
-      if (target_cpu == "mipsel" && is_component_build && is_android) {
+      if (current_os == "qnx") {
+        rspfile_content = "-Wl,--whole-archive {{inputs}} {{solibs}} -Wl,--no-whole-archive"
+      } else if (target_cpu == "mipsel" && is_component_build && is_android) {
         rspfile_content = "-Wl,--start-group -Wl,--whole-archive {{inputs}} {{solibs}} -Wl,--no-whole-archive {{libs}} -Wl,--end-group"
       } else if (current_os == "aix") {
         # --whole-archive, --no-whole-archive flags are not available on the aix
@@ -591,7 +612,16 @@ template("single_gcc_toolchain") {
       # We need to specify link groups, at least, for single pass linkers. I.e.
       # Rust libraries are alpha-sorted instead of by dependencies so they fail
       # to link if not properly ordered or grouped.
-      link_command = "$ld {{ldflags}}${extra_ldflags} -o \"$unstripped_outfile\" $start_group_flag @\"$rspfile\" $end_group_flag {{solibs}} {{libs}} $start_group_flag {{rlibs}} $end_group_flag"
+      if (current_os == "qnx") {
+        # QNX's single-pass linker needs all inputs in one --start-group so it
+        # can revisit archives when resolving symbols. Rust rlibs reference C++
+        # symbols from static libraries inside the rspfile, and vice versa.
+        link_command =
+            "$ld {{ldflags}}${extra_ldflags} -o \"$unstripped_outfile\" $start_group_flag @\"$rspfile\" {{solibs}} {{rlibs}} {{libs}} $end_group_flag"
+      } else {
+        link_command =
+            "$ld {{ldflags}}${extra_ldflags} -o \"$unstripped_outfile\" $start_group_flag @\"$rspfile\" $end_group_flag {{solibs}} {{libs}} $start_group_flag {{rlibs}} $end_group_flag"
+      }
 
       # Generate a map file to be used for binary size analysis.
       # Map file adds ~10% to the link time on a z620.
