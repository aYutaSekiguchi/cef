diff --git BUILD.gn BUILD.gn
index 0cb785515c..2caa7d101b 100644
--- BUILD.gn
+++ BUILD.gn
@@ -704,7 +704,7 @@ angle_static_library("angle_gpu_info_util") {
     libs += [ "dxgi.lib" ]
   }
 
-  if (is_linux || is_chromeos) {
+  if (is_linux || is_qnx || is_chromeos) {
     sources += libangle_gpu_info_util_linux_sources
 
     if (angle_use_x11 && angle_has_build) {
