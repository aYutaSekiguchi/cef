diff --git components/supervised_user/buildflags.gni components/supervised_user/buildflags.gni
index 7bb1f5758b0a3..fa7170b96ccf0 100644
--- components/supervised_user/buildflags.gni
+++ components/supervised_user/buildflags.gni
@@ -6,5 +6,5 @@ declare_args() {
   # Enables supervision for Family Link users.
   enable_supervised_users =
       # Platforms which fully support supervision features:
-      is_android || is_chromeos || is_ios || is_linux || is_mac || is_win
+      is_android || is_chromeos || is_ios || is_linux || is_mac || is_win || is_qnx
 }
diff --git content/gpu/BUILD.gn content/gpu/BUILD.gn
index 052263865c55c..6f9d633993612 100644
--- content/gpu/BUILD.gn
+++ content/gpu/BUILD.gn
@@ -125,7 +125,7 @@ target(link_target_type, "gpu_sources") {
 
   # Use DRI on desktop Linux builds.
   if (current_cpu != "s390x" && current_cpu != "ppc64" && is_linux &&
-      !is_castos) {
+      !is_castos && !is_qnx) {
     configs += [ "//build/config/linux/dri" ]
   }
 }
diff --git content/test/BUILD.gn content/test/BUILD.gn
index 605f7c158f128..b6325df65b5bf 100644
--- content/test/BUILD.gn
+++ content/test/BUILD.gn
@@ -3226,7 +3226,7 @@ test("content_unittests") {
     ]
   }
 
-  if (is_android || is_linux || is_chromeos || is_mac || is_win || is_fuchsia) {
+  if (is_android || is_linux || is_chromeos || is_mac || is_win || is_fuchsia || is_qnx) {
     data = [
       "$root_out_dir/content_shell.pak",
       "data/",
diff --git media/gpu/sandbox/BUILD.gn media/gpu/sandbox/BUILD.gn
index 7987b932c6eb4..0f9f2f9e37bbb 100644
--- media/gpu/sandbox/BUILD.gn
+++ media/gpu/sandbox/BUILD.gn
@@ -26,7 +26,7 @@ source_set("sandbox") {
     deps += [ "//media/gpu/v4l2" ]
   }
   if (current_cpu != "s390x" && current_cpu != "ppc64" && is_linux &&
-      !is_castos) {
+      !is_castos && !is_qnx) {
     # For DRI_DRIVER_DIR.
     configs += [ "//build/config/linux/dri" ]
   }
diff --git pdf/BUILD.gn pdf/BUILD.gn
index 5c85ee259b622..996af3868c2fc 100644
--- pdf/BUILD.gn
+++ pdf/BUILD.gn
@@ -233,6 +233,11 @@ if (enable_pdf) {
       "//ui/gfx/range",
     ]
 
+    if (is_qnx) {
+      deps -= [ "//printing" ]
+      public_deps -= [ "//printing/mojom" ]
+    }
+
     if (is_linux || is_chromeos) {
       sources += [
         "pdfium/pdfium_font_helpers.cc",
@@ -442,6 +447,10 @@ if (enable_pdf) {
       "//ui/gfx/range",
       "//v8",
     ]
+
+    if (is_qnx) {
+      deps -= [ "//printing" ]
+    }
   }
 
   source_set("pdf_test_utils") {
@@ -486,6 +495,10 @@ if (enable_pdf) {
       "//ui/gfx/range",
       "//ui/latency",
     ]
+
+    if (is_qnx) {
+      deps -= [ "//printing" ]
+    }
   }
 
   test("pdf_unittests") {
@@ -574,6 +587,10 @@ if (enable_pdf) {
       "//v8",
     ]
 
+    if (is_qnx) {
+      deps -= [ "//printing" ]
+    }
+
     data_deps = [
       ":pdf_unittests_pak",
       "//ui/resources:ui_test_pak_data",
diff --git third_party/abseil-cpp/absl/base/internal/low_level_alloc.cc third_party/abseil-cpp/absl/base/internal/low_level_alloc.cc
index a5bd71da6de5a..162748143bc5c 100644
--- third_party/abseil-cpp/absl/base/internal/low_level_alloc.cc
+++ third_party/abseil-cpp/absl/base/internal/low_level_alloc.cc
@@ -61,6 +61,10 @@
 #include "absl/base/internal/raw_logging.h"
 #include "absl/base/internal/spinlock.h"
 
+#if defined(__QNX__)
+#include "build/config/qnx/qnx_macros.h"
+#endif
+
 #if defined(MAP_ANON) && !defined(MAP_ANONYMOUS)
 #define MAP_ANONYMOUS MAP_ANON
 #endif
diff --git third_party/fontconfig/BUILD.gn third_party/fontconfig/BUILD.gn
index 58e3e4e802edb..312c213f01f4c 100644
--- third_party/fontconfig/BUILD.gn
+++ third_party/fontconfig/BUILD.gn
@@ -77,13 +77,32 @@ if (use_bundled_fontconfig) {
   }
 
   config("bindgen_config") {
-    include_dirs = [ "include" ]
+    include_dirs = [
+      "include",
+      "//third_party/llvm-build/Release+Asserts/lib/clang/23/include",
+    ]
     defines = [
       "BINDGEN_IGNORE_VISIBILITY=1",
       "FC_NO_MT=1",
       "FLEXIBLE_ARRAY_MEMBER=1",
       "HAVE_VASPRINTF=1",
     ]
+    if (is_qnx) {
+      defines += [
+        "__QNXNTO__",
+        "__QNX__",
+        "QNX_LIBM_BUILTINS",
+        "__LITTLEENDIAN__",
+        "__EXT_XOPEN_EX",
+        "_QNX_SOURCE",
+        "_POSIX_C_SOURCE=200809L",
+      ]
+      if (current_cpu == "x64") {
+        defines += [ "__X86_64__" ]
+      } else if (current_cpu == "arm64") {
+        defines += [ "__AARCH64__" ]
+      }
+    }
   }
 
   component("fontconfig") {
@@ -160,7 +179,7 @@ if (use_bundled_fontconfig) {
       ]
     }
 
-    if (!is_win) {
+    if (!is_win && !is_qnx) {
       libs = [ "uuid" ]
     }
   }
diff --git third_party/fontconfig/include/meson-config.h third_party/fontconfig/include/meson-config.h
index d9e3d64276b37..b126194d1a4b5 100644
--- third_party/fontconfig/include/meson-config.h
+++ third_party/fontconfig/include/meson-config.h
@@ -11,7 +11,9 @@
 
 #define CONFIGDIR "/etc/fonts/conf.d"
 
+#if !defined(__QNX__)
 #define ENABLE_NLS 1
+#endif
 
 #define EXEEXT 
 
@@ -94,9 +96,10 @@
 
 #define HAVE_RAND 1
 
+#if !defined(__QNX__)
 #define HAVE_RANDOM 1
-
 #define HAVE_RANDOM_R 1
+#endif
 
 #define HAVE_RAND_R 1
 
@@ -120,23 +123,23 @@
 
 #define HAVE_STRING_H 1
 
+#if !defined(__QNX__)
 #define HAVE_STRUCT_DIRENT_D_TYPE 1
-
 #define HAVE_STRUCT_STATFS_F_FLAGS 1
-
-#define HAVE_SYS_MOUNT_H 1
-
-#define HAVE_SYS_PARAM_H 1
-
 #define HAVE_SYS_STATFS_H 1
 
 #define HAVE_SYS_STATVFS_H 1
+#define HAVE_SYS_MOUNT_H 1
+#define HAVE_SYS_PARAM_H 1
+#endif
 
 #define HAVE_SYS_STAT_H 1
 
 #define HAVE_SYS_TYPES_H 1
 
+#if !defined(__QNX__)
 #define HAVE_SYS_VFS_H 1
+#endif
 
 #define HAVE_TIME_H 1
 
diff --git third_party/google_benchmark/BUILD.gn third_party/google_benchmark/BUILD.gn
index 6d1e9b1dd35bf..18c12477d686e 100644
--- third_party/google_benchmark/BUILD.gn
+++ third_party/google_benchmark/BUILD.gn
@@ -79,7 +79,7 @@ component("google_benchmark") {
     "BENCHMARK_VERSION=\"1.9.0\"",
   ]
 
-  if (is_linux) {
+  if (is_linux && !is_qnx) {
     # Include hardware performance counter support.
     defines += [ "HAVE_LIBPFM=1" ]
     deps = [ "//third_party/libpfm4" ]
