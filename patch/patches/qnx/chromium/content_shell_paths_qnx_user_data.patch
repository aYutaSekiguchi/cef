diff --git content/shell/common/shell_paths.cc content/shell/common/shell_paths.cc
--- content/shell/common/shell_paths.cc
+++ content/shell/common/shell_paths.cc
@@ -43,6 +43,8 @@ bool GetDefaultUserDataDirectory(base::FilePath* result) {
 #elif BUILDFLAG(IS_FUCHSIA)
   *result = base::FilePath(base::kPersistedDataDirectoryPath)
                 .Append(FILE_PATH_LITERAL("content_shell"));
+#elif BUILDFLAG(IS_QNX)
+  *result = base::FilePath(FILE_PATH_LITERAL("/tmp/content_shell"));
 #else
   NOTIMPLEMENTED();
   return false;
