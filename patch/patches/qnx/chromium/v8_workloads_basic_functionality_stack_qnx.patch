--- a/v8/test/unittests/heap/cppgc/workloads-unittest.cc
+++ b/v8/test/unittests/heap/cppgc/workloads-unittest.cc
@@ -5,6 +5,7 @@
 #include <algorithm>
 #include <iterator>
 #include <numeric>
+#include <vector>
 
 #include "include/cppgc/allocation.h"
 #include "include/cppgc/heap-consistency.h"
@@ -282,7 +283,8 @@ TEST_F(WorkloadsTest, BasicFunctionality) {
 
   size_t persistent_count = 0;
   const size_t kNumPersistents = 100000;
-  Persistent<DynamicallySizedObject>* persistents[kNumPersistents];
+  std::vector<Persistent<DynamicallySizedObject>*> persistents;
+  persistents.reserve(kNumPersistents);
 
   for (int i = 0; i < 1000; i++) {
     size_t size = 128 + i * 8;
