diff --git services/device/usb/BUILD.gn services/device/usb/BUILD.gn
--- services/device/usb/BUILD.gn
+++ services/device/usb/BUILD.gn
@@ -113,21 +113,21 @@ source_set("usb") {
     deps += [ "//third_party/libusb" ]
   }
 
-  if (is_linux || is_chromeos) {
+  if ((is_linux || is_chromeos) && !is_qnx) {
     sources += [
       "usb_device_linux.cc",
       "usb_device_linux.h",
     ]
   }
 
   if (use_udev) {
-    if (is_linux || is_chromeos) {
+    if ((is_linux || is_chromeos) && !is_qnx) {
       sources += [
         "usb_service_linux.cc",
         "usb_service_linux.h",
       ]
@@ -130,14 +130,14 @@ source_set("usb") {
     deps += [ "//device/udev_linux" ]
   }
 
-  if (is_android || is_chromeos || is_linux) {
+  if (is_android || is_chromeos || (is_linux && !is_qnx)) {
     sources += [
       "usb_device_handle_usbfs.cc",
       "usb_device_handle_usbfs.h",
     ]
   }
 
-  if (is_android || is_linux) {
+  if (is_android || (is_linux && !is_qnx)) {
     sources += [
       "usb_interface_detach_allowlist.cc",
       "usb_interface_detach_allowlist.h",
