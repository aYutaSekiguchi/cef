diff --git third_party/llvm-10.0/llvm/include/llvm/BinaryFormat/ELF.h third_party/llvm-10.0/llvm/include/llvm/BinaryFormat/ELF.h
index 25abbb2e0..1158c0095 100644
--- third_party/llvm-10.0/llvm/include/llvm/BinaryFormat/ELF.h
+++ third_party/llvm-10.0/llvm/include/llvm/BinaryFormat/ELF.h
@@ -19,6 +19,22 @@
 #ifndef LLVM_BINARYFORMAT_ELF_H
 #define LLVM_BINARYFORMAT_ELF_H
 
+#if defined(__QNX__)
+  // QNX SDP 8's sysroot headers (<elfdefinitions.h>, <sys/exec.h>,
+  // <sys/elf.h>, ...) define several ELF identifier names as
+  // preprocessor macros. The build's qnx_macros.h no longer drags
+  // <sys/elf.h> into every TU, but transitively-included QNX headers
+  // can still leave these macros defined when this header is pulled
+  // in. Undef them so the enums below parse.
+  #undef EV_NONE
+  #undef EV_CURRENT
+  #undef ELFOSABI_LINUX
+  #undef PT_ARM_UNWIND
+  #undef GRP_COMDAT
+  #undef GRP_MASKOS
+  #undef GRP_MASKPROC
+#endif
+
 #include <cstdint>
 #include <cstring>

