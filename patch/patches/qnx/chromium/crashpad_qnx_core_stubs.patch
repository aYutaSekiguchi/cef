diff --git third_party/crashpad/crashpad/util/misc/address_types.h third_party/crashpad/crashpad/util/misc/address_types.h
index c43d36cdf851d..5f0d9b94e143a 100644
--- third_party/crashpad/crashpad/util/misc/address_types.h
+++ third_party/crashpad/crashpad/util/misc/address_types.h
@@ -15,6 +15,7 @@
 #ifndef CRASHPAD_UTIL_MISC_ADDRESS_TYPES_H_
 #define CRASHPAD_UTIL_MISC_ADDRESS_TYPES_H_
 
+#include <stddef.h>
 #include <stdint.h>
 
 #include <type_traits>
@@ -27,6 +28,7 @@
 #include "util/win/address_types.h"
 #elif BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID)
 #include "util/linux/address_types.h"
+#elif BUILDFLAG(IS_QNX)
 #elif BUILDFLAG(IS_FUCHSIA)
 #include <zircon/types.h>
 #else
@@ -60,6 +62,11 @@ using VMSize = WinVMSize;
 using VMAddress = LinuxVMAddress;
 using VMSize = LinuxVMSize;
 
+#elif BUILDFLAG(IS_QNX)
+
+using VMAddress = uintptr_t;
+using VMSize = size_t;
+
 #elif BUILDFLAG(IS_FUCHSIA)
 
 using VMAddress = zx_vaddr_t;
diff --git third_party/crashpad/crashpad/minidump/minidump_misc_info_writer.cc third_party/crashpad/crashpad/minidump/minidump_misc_info_writer.cc
index 1abb46c318142..d0418feb05249 100644
--- third_party/crashpad/crashpad/minidump/minidump_misc_info_writer.cc
+++ third_party/crashpad/crashpad/minidump/minidump_misc_info_writer.cc
@@ -155,6 +155,8 @@ std::string MinidumpMiscInfoDebugBuildString() {
   static constexpr char kOS[] = "android";
 #elif BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS)
   static constexpr char kOS[] = "linux";
+#elif BUILDFLAG(IS_QNX)
+  static constexpr char kOS[] = "qnx";
 #elif BUILDFLAG(IS_WIN)
   static constexpr char kOS[] = "win";
 #elif BUILDFLAG(IS_FUCHSIA)
diff --git third_party/crashpad/crashpad/handler/handler_main.cc third_party/crashpad/crashpad/handler/handler_main.cc
index 0a457a6542397..70a3231d9db2c 100644
--- third_party/crashpad/crashpad/handler/handler_main.cc
+++ third_party/crashpad/crashpad/handler/handler_main.cc
@@ -68,6 +68,10 @@
 #include "handler/linux/crash_report_exception_handler.h"
 #include "handler/linux/exception_handler_server.h"
 #include "util/posix/signals.h"
+#elif BUILDFLAG(IS_QNX)
+#include "handler/qnx/crash_report_exception_handler.h"
+#include "handler/qnx/exception_handler_server.h"
+#include "util/posix/signals.h"
 #elif BUILDFLAG(IS_APPLE)
 #include <libgen.h>
 #include <signal.h>
@@ -328,7 +332,7 @@ class CallMetricsRecordNormalExit {
 };
 
 #if BUILDFLAG(IS_APPLE) || BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || \
-    BUILDFLAG(IS_ANDROID)
+    BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_QNX)
 
 void HandleCrashSignal(int sig, siginfo_t* siginfo, void* context) {
   MetricsRecordExit(Metrics::LifetimeMilestone::kCrashed);
@@ -1079,7 +1083,7 @@ int HandlerMain(int argc,
     upload_thread.Get()->Start();
   }
 
-#if BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID)
+#if BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_QNX)
   std::unique_ptr<ExceptionHandlerServer::Delegate> exception_handler;
 #else
   std::unique_ptr<CrashReportExceptionHandler> exception_handler;
@@ -1206,6 +1210,8 @@ int HandlerMain(int argc,
   }
 #elif BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_ANDROID)
   ExceptionHandlerServer exception_handler_server;
+#elif BUILDFLAG(IS_QNX)
+  ExceptionHandlerServer exception_handler_server;
 #endif  // BUILDFLAG(IS_APPLE)
 
   base::GlobalHistogramAllocator* histogram_allocator = nullptr;
