diff --git skia/BUILD.gn skia/BUILD.gn
index 31fad5923ad2df81559c7acefe69fe2ec0867cef..85adff4a1fcadd2bc3eb2cab4ffdc5b10954fd4e 100644
--- skia/BUILD.gn
+++ skia/BUILD.gn
@@ -104,6 +104,23 @@ config("skia_config") {
       "SK_GAMMA_EXPONENT=1.2",
       "SK_GAMMA_CONTRAST=0.2",
     ]
+  } else if (is_qnx) {
+    # QNX is `is_linux = false` on the is_linux=false experiment branch, so
+    # it no longer falls into the Linux/ChromeOS block above. QNX does not
+    # define __linux / __GLIBC__ / __unix__, so third_party/skia's
+    # include/private/base/SkFeatures.h platform auto-detection would
+    # otherwise fall into its `else` branch and define SK_BUILD_FOR_MAC.
+    # That would make `src/utils/mac/SkCTFont.cpp` and friends (kept in
+    # skia_utils_private unconditionally) #import
+    # ApplicationServices/ApplicationServices.h and
+    # src/base/SkSemaphore.cpp #include <dispatch/dispatch.h>, neither of
+    # which QNX SDP 8 ships. Pre-define SK_BUILD_FOR_UNIX so the auto-detect
+    # block is skipped entirely and QNX reuses the Linux gamma settings.
+    defines += [
+      "SK_BUILD_FOR_UNIX",
+      "SK_GAMMA_EXPONENT=1.2",
+      "SK_GAMMA_CONTRAST=0.2",
+    ]
   } else if (is_android) {
     defines += [
       "SK_BUILD_FOR_ANDROID",
@@ -439,7 +456,7 @@ component("skia") {
     sources += skia_ports_fontmgr_coretext_sources
   }
 
-  if (is_linux || is_chromeos) {
+  if (is_linux || is_chromeos || is_qnx) {
     public += skia_ports_fci_public
     sources += skia_ports_fci_sources
   }
@@ -504,7 +521,7 @@ component("skia") {
     }
   }
 
-  if (is_linux || is_chromeos) {
+  if (is_linux || is_chromeos || is_qnx) {
     deps += [
       "//third_party/expat",
       "//third_party/fontconfig",
diff --git skia/ext/font_utils.cc skia/ext/font_utils.cc
index 15ef5c562f8d51098c837b5c46e38f327d516e34..7adb17188bda301a0daac257e4df52933d1c53be 100644
--- skia/ext/font_utils.cc
+++ skia/ext/font_utils.cc
@@ -25,7 +25,7 @@
 #include "third_party/skia/include/ports/SkFontMgr_mac_ct.h"
 #endif
 
-#if BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_LINUX)
+#if BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_QNX)
 #include "third_party/skia/include/ports/SkFontConfigInterface.h"
 #include "third_party/skia/include/ports/SkFontMgr_FontConfigInterface.h"
 #include "third_party/skia/include/ports/SkFontScanner_Fontations.h"
@@ -83,7 +83,7 @@ static sk_sp<SkFontMgr> fontmgr_factory() {
   return SkFontMgr_New_Android(nullptr, SkFontScanner_Make_Fontations());
 #elif BUILDFLAG(IS_APPLE)
   return SkFontMgr_New_CoreText(nullptr);
-#elif BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_LINUX)
+#elif BUILDFLAG(IS_CHROMEOS) || BUILDFLAG(IS_LINUX) || BUILDFLAG(IS_QNX)
   sk_sp<SkFontConfigInterface> fci(SkFontConfigInterface::RefGlobal());
   return fci ? SkFontMgr_New_FCI(std::move(fci),
                                  SkFontScanner_Make_Fontations())
