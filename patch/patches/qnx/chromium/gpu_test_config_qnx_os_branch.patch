diff --git gpu/config/gpu_test_config.cc gpu/config/gpu_test_config.cc
index 64e672a2ede06..b2a90f2a02070 100644
--- gpu/config/gpu_test_config.cc
+++ gpu/config/gpu_test_config.cc
@@ -87,6 +87,10 @@ GPUTestConfig::OS GetCurrentOS() {
   return GPUTestConfig::kOsFuchsia;
 #elif BUILDFLAG(IS_IOS)
   return GPUTestConfig::kOsIOS;
+#elif BUILDFLAG(IS_QNX)
+  // QNX is routed through is_linux by GN; report kOsLinux as a neutral
+  // placeholder so the gpu_test_config enumeration compiles on QNX.
+  return GPUTestConfig::kOsLinux;
 #else
 #error "unknown os"
 #endif
