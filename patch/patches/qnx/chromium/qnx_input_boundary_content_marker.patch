diff --git browser/renderer_host/render_widget_host_view_aura.cc browser/renderer_host/render_widget_host_view_aura.cc
index 95f75a24fa3e799dc4227e6438b1d0cc316ba4b9..9fd7a9fa253b7b539baaff7072ab1c93f4c32a55 100644
@@ -2395,6 +2395,14 @@
 }
 
 void RenderWidgetHostViewAura::OnMouseEvent(ui::MouseEvent* event) {
+
+#if BUILDFLAG(IS_QNX)
+  static bool qnx_rwhva_onmouse_one_shot = true;
+  if (qnx_rwhva_onmouse_one_shot) {
+    qnx_rwhva_onmouse_one_shot = false;
+    LOG(INFO) << "[QNX_INPUT_BOUNDARY] content=RenderWidgetHostViewAura::OnMouseEvent reached";
+  }
+#endif
 #if BUILDFLAG(IS_WIN)
   if (event->type() == ui::EventType::kMouseMoved) {
     if (event->location() == last_mouse_move_location_ &&
