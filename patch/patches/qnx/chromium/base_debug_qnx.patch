diff --git a/base/debug/debugger_posix.cc b/base/debug/debugger_posix.cc
index d5dc8c41319e8..97147ca714739 100644
--- a/base/debug/debugger_posix.cc
+++ b/base/debug/debugger_posix.cc
@@ -243,6 +243,17 @@ void VerifyDebugger() {
 #endif
 }
 
+#elif BUILDFLAG(IS_QNX)
+
+bool BeingDebugged() {
+  // QNX does not provide Linux's /proc/self/status TracerPid interface.
+  // This function is also called from the in-process stack-dump signal handler,
+  // so it must not log, allocate, or call non-async-signal-safe APIs there.
+  return false;
+}
+
+void VerifyDebugger() {}
+
 #else
 
 bool BeingDebugged() {
diff --git a/base/debug/elf_reader.cc b/base/debug/elf_reader.cc
index 5d2a0c2779b08..4119e89b22e9a 100644
--- a/base/debug/elf_reader.cc
+++ b/base/debug/elf_reader.cc
@@ -5,7 +5,11 @@
 #include "base/debug/elf_reader.h"
 
 #include <arpa/inet.h>
+#if BUILDFLAG(IS_QNX)
+#include <sys/elf.h>
+#else
 #include <elf.h>
+#endif
 #include <string.h>
 
 #include <optional>
@@ -160,11 +164,12 @@ std::optional<std::string_view> ReadElfLibraryName(
          UNSAFE_TODO(++dynamic_iter)) {
       if (dynamic_iter->d_tag == DT_STRTAB) {
 #if BUILDFLAG(IS_FUCHSIA) || BUILDFLAG(IS_ANDROID) || \
+    BUILDFLAG(IS_QNX) || \
     (defined(ARCH_CPU_RISCV_FAMILY) && BUILDFLAG(IS_LINUX))
-        // Fuchsia and Android do not relocate the strtab pointer on ELF load.
-        // When the dynamic section is readonly, the strtab pointer is also not
-        // relocated. This is the default ABI behavior on RISC-V GNU Linux (See
-        // sysdeps/riscv/dl-relocate-ld.h).
+        // Fuchsia, Android and QNX do not relocate the strtab pointer on ELF
+        // load. When the dynamic section is readonly, the strtab pointer is
+        // also not relocated. This is the default ABI behavior on RISC-V GNU
+        // Linux (See sysdeps/riscv/dl-relocate-ld.h).
         strtab_addr =
             UNSAFE_TODO(static_cast<size_t>(dynamic_iter->d_un.d_ptr) +
                         reinterpret_cast<const char*>(relocation_offset));
diff --git a/base/debug/elf_reader.h b/base/debug/elf_reader.h
index a48472405173b..d15793ac59d1f 100644
--- a/base/debug/elf_reader.h
+++ b/base/debug/elf_reader.h
@@ -5,7 +5,13 @@
 #ifndef BASE_DEBUG_ELF_READER_H_
 #define BASE_DEBUG_ELF_READER_H_
 
+#include "build/build_config.h"
+
+#if BUILDFLAG(IS_QNX)
+#include <sys/elf.h>
+#else
 #include <elf.h>
+#endif
 
 #include <optional>
 #include <string_view>
diff --git a/base/debug/elf_reader_unittest.cc b/base/debug/elf_reader_unittest.cc
index 9be01511199db..5fe7392656bed 100644
--- a/base/debug/elf_reader_unittest.cc
+++ b/base/debug/elf_reader_unittest.cc
@@ -181,7 +181,7 @@ TEST(ElfReaderTestWithCurrentElfImage, ReadElfBuildId) {
   size_t build_id_size = ReadElfBuildId(&__executable_start, true, build_id);
   ASSERT_NE(build_id_size, 0u);
 
-#if defined(OFFICIAL_BUILD)
+#if defined(OFFICIAL_BUILD) || BUILDFLAG(IS_QNX)
   constexpr size_t kExpectedBuildIdStringLength = 40;  // SHA1 hash in hex.
 #else
   constexpr size_t kExpectedBuildIdStringLength = 16;  // 64-bit int in hex.
diff --git a/base/debug/stack_trace.cc b/base/debug/stack_trace.cc
index ebde49a9ffe9c..ed311def6e9db 100644
--- a/base/debug/stack_trace.cc
+++ b/base/debug/stack_trace.cc
@@ -306,10 +306,10 @@ bool StackTrace::WillSymbolizeToStreamForTesting() {
   // Official Mac OS X builds contain enough information to unwind the stack,
   // but not enough to symbolize the output.
   return false;
-#elif BUILDFLAG(IS_FUCHSIA) || BUILDFLAG(IS_ANDROID)
-  // Under Fuchsia and Android, StackTrace emits executable build-Ids and
-  // address offsets which are symbolized on the test host system, rather than
-  // being symbolized in-process.
+#elif BUILDFLAG(IS_FUCHSIA) || BUILDFLAG(IS_ANDROID) || BUILDFLAG(IS_QNX)
+  // Under Fuchsia, Android and QNX, StackTrace emits raw addresses which
+  // are symbolized on the test host system, rather than being symbolized
+  // in-process.
   return false;
 #elif BUILDFLAG(PRINT_UNSYMBOLIZED_STACK_TRACES)
   // Typically set in sanitizer configurations (ASan, TSan, MSan), which emit
diff --git a/base/debug/test_elf_image_builder.cc b/base/debug/test_elf_image_builder.cc
index 027ad0f959f7e..5cd29ad2c102c 100644
--- a/base/debug/test_elf_image_builder.cc
+++ b/base/debug/test_elf_image_builder.cc
@@ -271,9 +271,10 @@ TestElfImage TestElfImageBuilder::Build() {
   Dyn strtab_dyn;
   strtab_dyn.d_tag = DT_STRTAB;
 #if BUILDFLAG(IS_FUCHSIA) || BUILDFLAG(IS_ANDROID) || \
+    BUILDFLAG(IS_QNX) || \
     (defined(ARCH_CPU_RISCV_FAMILY) && BUILDFLAG(IS_LINUX))
-  // Fuchsia and Android do not alter the symtab pointer on ELF load -- it's
-  // expected to remain a 'virutal address'.
+  // Fuchsia, Android and QNX do not alter the symtab pointer on ELF load
+  // -- it's expected to remain a 'virtual address'.
   strtab_dyn.d_un.d_ptr =
       GetVirtualAddressForOffset(measures.strtab_start, elf_start);
 #else
diff --git a/base/debug/test_elf_image_builder.h b/base/debug/test_elf_image_builder.h
index 7dfee81fab428..31c1d0e428935 100644
--- a/base/debug/test_elf_image_builder.h
+++ b/base/debug/test_elf_image_builder.h
@@ -5,7 +5,13 @@
 #ifndef BASE_DEBUG_TEST_ELF_IMAGE_BUILDER_H_
 #define BASE_DEBUG_TEST_ELF_IMAGE_BUILDER_H_
 
+#include "build/build_config.h"
+
+#if BUILDFLAG(IS_QNX)
+#include <sys/elf.h>
+#else
 #include <elf.h>
+#endif
 
 #include <cstdint>
 #include <optional>
