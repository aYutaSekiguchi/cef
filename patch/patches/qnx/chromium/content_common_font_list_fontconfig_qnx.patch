diff --git content/common/BUILD.gn content/common/BUILD.gn
--- content/common/BUILD.gn
+++ content/common/BUILD.gn
@@ -393,7 +393,7 @@ source_set("common") {
     ]
   }

-  if (is_mac || is_win || is_android || is_fuchsia || is_ios) {
+  if (is_mac || is_win || is_android || is_fuchsia || is_ios || is_qnx) {
     sources -= [ "font_list_fontconfig.cc" ]
   }

