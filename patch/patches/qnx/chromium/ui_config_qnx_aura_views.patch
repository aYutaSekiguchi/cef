diff --git a/build/config/ui.gni b/build/config/ui.gni
index 529d1c55f5b39..1121f099226b0 100644
--- a/build/config/ui.gni
+++ b/build/config/ui.gni
@@ -25,12 +25,13 @@ import("//build/config/ozone.gni")
 declare_args() {
   # Indicates if Aura is enabled. Aura is a low-level windowing library, sort
   # of a replacement for GDI or GTK.
-  use_aura = is_win || is_linux || is_chromeos || is_fuchsia
+  use_aura = is_win || is_linux || is_chromeos || is_fuchsia || is_qnx
 }
 
 declare_args() {
   # True means the UI is built using the "views" framework.
-  toolkit_views = is_mac || is_win || is_linux || is_chromeos || is_fuchsia
+  toolkit_views =
+      is_mac || is_win || is_linux || is_chromeos || is_fuchsia || is_qnx
 
   use_glib =
       is_linux && !is_castos &&
