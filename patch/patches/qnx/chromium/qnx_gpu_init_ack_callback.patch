diff --git ui/ozone/platform/qnx/mojom/qnx_gpu.mojom ui/ozone/platform/qnx/mojom/qnx_gpu.mojom
index a321022e0fdfb7e1e56e6b57224a3c4cc507e164..da49fc9943973f4bc1fa9345e041fd8a17b6206f 100644
--- ui/ozone/platform/qnx/mojom/qnx_gpu.mojom
+++ ui/ozone/platform/qnx/mojom/qnx_gpu.mojom
@@ -99,7 +99,13 @@ interface QnxGpuService {
   // |host_remote| is a PendingRemote<QnxGpuHost> owned by the Browser.
   // The GPU process receives only the client end and stores it as a
   // mojo::Remote<QnxGpuHost>.
-  Initialize(pending_remote<QnxGpuHost> host_remote);
+  // Returns ack after the GPU has bound |host_remote|.  The browser uses
+  // this happens-before edge to serialize the QnxGpuService pipe (Initialize)
+  // and the QnxGpuControl pipe (AttachWidget) before sending any
+  // AttachWidget calls.  Mojo does not guarantee ordering across pipes,
+  // so without this ack the GPU may dispatch AttachWidget before
+  // Initialize and silently drop the SubmitFrame trigger.
+  Initialize(pending_remote<QnxGpuHost> host_remote) => ();
 };
 

diff --git ui/ozone/platform/qnx/qnx_gpu_platform_support_host.cc ui/ozone/platform/qnx/qnx_gpu_platform_support_host.cc
index 5f54db994ed1fc86017432993bc6aa082f535fed..eb36b64abd4dcc1939af1ee95d9f9fa8ea908521 100644
--- ui/ozone/platform/qnx/qnx_gpu_platform_support_host.cc
+++ ui/ozone/platform/qnx/qnx_gpu_platform_support_host.cc
@@ -123,16 +123,23 @@ void QnxGpuPlatformSupportHost::OnGpuServiceLaunched(
   // This hands the browser-owned QnxGpuHost remote to the GPU process.
   // After this, the GPU can call SubmitFrame and ReportProducerLost.
   // The QnxGpuControl pipe is bound to the GPU via the binder in Step 3.
-  gpu_service_remote_->Initialize(std::move(host_remote));
+  //
+  // Mojo does not guarantee ordering across pipes. AttachWidget calls
+  // (QnxGpuControl pipe) must not race Initialize (QnxGpuService pipe):
+  // if AttachWidget arrives at the GPU before Initialize, the GPU drops
+  // SubmitFrame because gpu_host_remote_ is not yet bound.  Move
+  // AttachExistingWidgets into the Initialize ack callback so the
+  // QnxGpuService pipe is fully established before any QnxGpuControl
+  // message is sent, per the mojom contract comment.
+  gpu_service_remote_->Initialize(
+      std::move(host_remote),
+      base::BindOnce(
+          &QnxGpuPlatformSupportHost::AttachExistingWidgets,
+          base::Unretained(this), host_id));
 
   DLOG(INFO) << "QnxGpuPlatformSupportHost::OnGpuServiceLaunched: "
                 "QnxGpuService::Initialize() called; GPU can now call "
                 "SubmitFrame and receive QnxGpuControl via AddInterfaces";
-
-  // ---- Step 5: Attach existing browser windows to the new GPU ----
-  // Iterate all browser-owned widgets created before the GPU connected
-  // and inform the GPU of their current state.
-  AttachExistingWidgets(host_id);
 }
 
 void QnxGpuPlatformSupportHost::OnChannelDestroyed(int host_id) {
   DCHECK_CALLED_ON_VALID_THREAD(ui_thread_checker_);
 

+      std::move(host_remote),
+      base::BindOnce(
+          &QnxGpuPlatformSupportHost::AttachExistingWidgets,
+          base::Unretained(this), host_id));
+
+  DLOG(INFO) << "QnxGpuPlatformSupportHost::OnGpuServiceLaunched: "
+                "QnxGpuService::Initialize() called; GPU can now call "
+                "SubmitFrame and receive QnxGpuControl via AddInterfaces";
 
   DLOG(INFO) << "QnxGpuPlatformSupportHost::OnGpuServiceLaunched: "
                 "QnxGpuService::Initialize() called; GPU can now call "
diff --git ui/ozone/platform/qnx/qnx_gpu_service.cc ui/ozone/platform/qnx/qnx_gpu_service.cc
index 3e36bc564175748d69626b73094d15bd4297ca0d..ec12a68949b9726c0c1a765dfb6bf0047d38b649 100644
--- ui/ozone/platform/qnx/qnx_gpu_service.cc
+++ ui/ozone/platform/qnx/qnx_gpu_service.cc
@@ -81,9 +81,12 @@ void QnxGpuService::BindQnxGpuControl(
 // ======================================================================
 
 void QnxGpuService::Initialize(
-    mojo::PendingRemote<qnx::QnxGpuHost> host_remote) {
+    mojo::PendingRemote<qnx::QnxGpuHost> host_remote,
+    InitializeCallback callback) {
   if (!host_remote) {
     DLOG(ERROR) << "QnxGpuService::Initialize: null host_remote";
+    if (callback)
+      std::move(callback).Run();
     return;
   }
 
@@ -109,6 +112,9 @@ void QnxGpuService::Initialize(
   DLOG(INFO) << "QnxGpuService::Initialize: gpu_host_remote bound; "
                 "GPU can now call SubmitFrame / ReportProducerLost; "
                 "QnxGpuControl receiver bound via binder in AddInterfaces";
+  // Phase 6: run the ack reply so the browser's pending BindOnce fires.
+  if (callback)
+    std::move(callback).Run();
   if (IsQnxGpuTraceEnabled()) {
     LOG(INFO) << "QNX_OZONE_GPU_TRACE QnxGpuService::Initialize: gpu_host_remote"
                  " bound; GPU process is ready to call SubmitFrame";
diff --git ui/ozone/platform/qnx/qnx_gpu_service.h ui/ozone/platform/qnx/qnx_gpu_service.h
index b275850300ab3ad48b90d162fd97abdaf5c564d0..257b4a19a34d31a9fad5790e5cd9590a0227ac18 100644
--- ui/ozone/platform/qnx/qnx_gpu_service.h
+++ ui/ozone/platform/qnx/qnx_gpu_service.h
@@ -78,7 +78,14 @@ class QnxGpuService : public qnx::QnxGpuService,
       mojo::PendingReceiver<qnx::QnxGpuControl> pending_receiver);
 
   // qnx::QnxGpuService:
-  void Initialize(mojo::PendingRemote<qnx::QnxGpuHost> host_remote) override;
+  // Phase 6 ack-style: the .mojom declares
+  //   Initialize(pending_remote<QnxGpuHost> host_remote) => ();
+  // so the override must accept the InitializeCallback reply argument.
+  // The browser caller currently passes a no-op callback; the deeper
+  // bind/attach refactor (BindGpuControlAndAttachExistingWidgets) is
+  // tracked separately.
+  void Initialize(mojo::PendingRemote<qnx::QnxGpuHost> host_remote,
+                  InitializeCallback callback) override;
 
   // qnx::QnxGpuControl: GPU-side handler for widget lifecycle messages
   // from the browser.  The browser calls these methods on its
