--- cef/patch/qnx/chromium/new_files/ui/ozone/platform/qnx/qnx_window_manager.h	2026-07-08 08:43:55.000000000 +0900
+++ ui/ozone/platform/qnx/qnx_window_manager.h	2026-07-08 15:13:28.000000000 +0900
@@ -18,6 +18,7 @@
 
 namespace ui {
 
+class QnxGpuPlatformSupportHost;
 class QnxWindow;
 
 // Per-widget record tracking GPU attachment and generation.
@@ -105,6 +106,12 @@
   // can be modified during iteration.
   std::vector<QnxWidgetRecord> GetWidgetRecordsForTestingOrGpuAttach() const;
 
+  // Wire up the QNX GPU platform support host so AddWindow can notify it of
+  // new widgets created AFTER the GPU service connects. Set once from
+  // OzonePlatformQnxImpl after both members are constructed.
+  void SetGpuPlatformSupportHost(
+      QnxGpuPlatformSupportHost* gpu_platform_support_host);
+
  private:
   // Singleton instance pointer. Set in constructor, cleared in destructor.
   // This is safe because there is only one OzonePlatform per process and
@@ -114,6 +121,11 @@
   base::IDMap<QnxWindow*> windows_;
   std::map<gfx::AcceleratedWidget, QnxWidgetRecord> records_;
   base::ThreadChecker thread_checker_;
+
+  // Non-owning pointer to the browser-side GPU support host. Set by
+  // OzonePlatformQnxImpl after both members are constructed; reset to null
+  // before destruction so AddWindow no longer fires after teardown begins.
+  raw_ptr<QnxGpuPlatformSupportHost> gpu_platform_support_host_ = nullptr;
 };
 
 }  // namespace ui

--- cef/patch/qnx/chromium/new_files/ui/ozone/platform/qnx/qnx_window_manager.cc	2026-07-08 08:43:55.000000000 +0900
+++ ui/ozone/platform/qnx/qnx_window_manager.cc	2026-07-08 15:13:28.000000000 +0900
@@ -7,6 +7,7 @@
 #include "base/check.h"
 #include "base/logging.h"
 #include "ui/gfx/geometry/rect.h"
+#include "ui/ozone/platform/qnx/qnx_gpu_platform_support_host.h"
 #include "ui/ozone/platform/qnx/qnx_window.h"
 
 namespace ui {
@@ -43,9 +44,24 @@
   records_[widget] = record;
 
   DLOG(INFO) << "QnxWindowManager: AddWindow widget=" << widget;
+
+  // If the GPU service has already connected, the initial
+  // AttachExistingWidgets() pass missed this widget (it didn't exist yet).
+  // Forward AttachWidget so the GPU process can create its producer for the
+  // newly-created screen_window_t. No-op if the GPU isn't connected yet.
+  if (gpu_platform_support_host_) {
+    gpu_platform_support_host_->AttachNewWidget(widget);
+  }
+
   return widget;
 }
 
+void QnxWindowManager::SetGpuPlatformSupportHost(
+    QnxGpuPlatformSupportHost* gpu_platform_support_host) {
+  DCHECK(thread_checker_.CalledOnValidThread());
+  gpu_platform_support_host_ = gpu_platform_support_host;
+}
+
 void QnxWindowManager::RemoveWindow(gfx::AcceleratedWidget widget,
                                     QnxWindow* window) {
   DCHECK(thread_checker_.CalledOnValidThread());

--- cef/patch/qnx/chromium/new_files/ui/ozone/platform/qnx/ozone_platform_qnx.cc	2026-07-08 08:43:55.000000000 +0900
+++ ui/ozone/platform/qnx/ozone_platform_qnx.cc	2026-07-08 15:13:28.000000000 +0900
@@ -185,6 +185,13 @@
     gpu_platform_support_host_ =
         std::make_unique<QnxGpuPlatformSupportHost>(window_manager_.get());
 
+    // Wire the support host back to the window manager so AddWindow can
+    // notify the GPU about widgets created AFTER the GPU service connects.
+    // AttachExistingWidgets (called from OnGpuServiceLaunched) only sees
+    // widgets that existed at GPU launch time; for content_shell about:blank
+    // the window is created later, so AttachNewWidget is required.
+    window_manager_->SetGpuPlatformSupportHost(gpu_platform_support_host_.get());
+
     // Phase 5: Create browser-local surface factory for ozone_demo software
     // canvas. This factory provides CreateCanvasForWidget() which wraps
     // screen_post_window pixel posting. GPU producer resources (GLOzone) are

--- cef/patch/qnx/chromium/new_files/ui/ozone/platform/qnx/qnx_gpu_platform_support_host.h	2026-07-08 08:43:55.000000000 +0900
+++ ui/ozone/platform/qnx/qnx_gpu_platform_support_host.h	2026-07-08 15:13:28.000000000 +0900
@@ -83,6 +83,14 @@
   // windows and send AttachWidget to the GPU for each one.
   void AttachExistingWidgets(int host_id);
 
+  // Called by QnxWindowManager::AddWindow when a new QnxWindow is created
+  // AFTER the GPU service has connected. AttachExistingWidgets runs once at
+  // OnGpuServiceLaunched and only sees widgets that existed at that time, so
+  // any widget created later (e.g., content_shell's about:blank window)
+  // must trigger its own AttachWidget. No-op when the GPU isn't connected
+  // yet (early-boot widget creation) or when the widget record is missing.
+  void AttachNewWidget(gfx::AcceleratedWidget widget);
+
  private:
   // Resets the GPU-side QnxGpuService remote. Called on channel destroyed.
   // Also marks all widgets as GPU-detached and increments generation.

--- cef/patch/qnx/chromium/new_files/ui/ozone/platform/qnx/qnx_gpu_platform_support_host.cc	2026-07-08 08:43:55.000000000 +0900
+++ ui/ozone/platform/qnx/qnx_gpu_platform_support_host.cc	2026-07-08 15:13:28.000000000 +0900
@@ -135,7 +135,6 @@
   AttachExistingWidgets(host_id);
 }
 
-
 void QnxGpuPlatformSupportHost::OnChannelDestroyed(int host_id) {
   DCHECK_CALLED_ON_VALID_THREAD(ui_thread_checker_);
 
@@ -238,6 +237,50 @@
   }
 }
 
+void QnxGpuPlatformSupportHost::AttachNewWidget(
+    gfx::AcceleratedWidget widget) {
+  DCHECK_CALLED_ON_VALID_THREAD(ui_thread_checker_);
+
+  // No GPU connected yet (gpu_control_remote_ not bound yet) -> AttachExistingWidgets
+  // will pick this widget up when the GPU launches
+  // fires. This is the common early-boot case.
+  if (!gpu_control_remote_) {
+    DLOG(INFO) << "AttachNewWidget: widget=" << widget
+               << " GPU not connected yet; AttachExistingWidgets will handle";
+    return;
+  }
+
+  if (!window_manager_) {
+    DLOG(WARNING) << "AttachNewWidget: no window_manager_";
+    return;
+  }
+
+  const QnxWidgetRecord* record = window_manager_->GetWidgetRecord(widget);
+  if (!record) {
+    DLOG(WARNING) << "AttachNewWidget: no record for widget=" << widget;
+    return;
+  }
+
+  if (record->gpu_attached) {
+    DLOG(WARNING) << "AttachNewWidget: widget=" << widget
+                   << " is already GPU-attached; skipping";
+    return;
+  }
+
+  // Same bookkeeping as AttachExistingWidgets: increment generation,
+  // mark GPU-attached, then read back the new generation.
+  window_manager_->IncrementGeneration(widget);
+  window_manager_->SetGpuAttached(widget, true, 0);
+  const QnxWidgetRecord* updated = window_manager_->GetWidgetRecord(widget);
+  uint32_t new_gen = updated ? updated->generation : 1u;
+
+  gpu_control_remote_->AttachWidget(widget, new_gen, record->size);
+
+  DLOG(INFO) << "AttachNewWidget: widget=" << widget
+             << " generation=0 -> " << new_gen
+             << " GPU-attached; AttachWidget(size=" << record->size.width()
+             << "x" << record->size.height() << ") sent to GPU";
+}
 
 // ======================================================================
 // Private helpers

