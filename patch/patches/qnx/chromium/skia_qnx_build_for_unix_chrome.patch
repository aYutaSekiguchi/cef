diff --git skia/BUILD.gn skia/BUILD.gn
index 31fad5923ad2df81559c7acefe69fe2ec0867cef..0000000000000000000000000000000000000000 100644
--- skia/BUILD.gn
+++ skia/BUILD.gn
@@ -103,6 +103,17 @@ config("skia_config") {
       "SK_GAMMA_EXPONENT=1.2",
       "SK_GAMMA_CONTRAST=0.2",
     ]
+    if (is_qnx) {
+      # QNX reaches this branch because Chromium's current QNX baseline sets
+      # `is_linux = current_os == "linux" || is_qnx`. QNX does not define
+      # __linux / __GLIBC__ / __unix__, so third_party/skia's
+      # include/private/base/SkFeatures.h platform auto-detection would
+      # otherwise fall into its `else` branch and define SK_BUILD_FOR_MAC.
+      # That would make `src/utils/mac/SkCTFont.cpp` and friends #import
+      # ApplicationServices/ApplicationServices.h, which QNX SDP 8 does not
+      # ship. Pre-define SK_BUILD_FOR_UNIX while keeping Linux gamma settings.
+      defines += [ "SK_BUILD_FOR_UNIX" ]
+    }
   } else if (is_android) {
     defines += [
       "SK_BUILD_FOR_ANDROID",
