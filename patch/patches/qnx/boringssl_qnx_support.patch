diff --git a/crypto/rand/internal.h b/crypto/rand/internal.h
index 9ed080f2a..f11d59662 100644
--- a/crypto/rand/internal.h
+++ b/crypto/rand/internal.h
@@ -25,6 +25,8 @@
 #define OPENSSL_RAND_WINDOWS
 #elif defined(OPENSSL_LINUX)
 #define OPENSSL_RAND_URANDOM
+#elif defined(__QNX__)
+#define OPENSSL_RAND_URANDOM
 #elif defined(OPENSSL_APPLE) && !defined(OPENSSL_MACOS)
 // Unlike macOS, iOS and similar hide away getentropy().
 #define OPENSSL_RAND_IOS
diff --git a/crypto/rand/urandom.cc b/crypto/rand/urandom.cc
index d06ade712..ccee54041 100644
--- a/crypto/rand/urandom.cc
+++ b/crypto/rand/urandom.cc
@@ -25,9 +25,11 @@
 
 #include <errno.h>
 #include <fcntl.h>
+#if defined(OPENSSL_LINUX)
 #include <linux/random.h>
-#include <stdio.h>
 #include <sys/syscall.h>
+#endif
+#include <stdio.h>
 #include <unistd.h>
 
 #include "../internal.h"
@@ -41,6 +43,7 @@ void __msan_unpoison(void *, size_t);
 }
 #endif
 
+#if defined(OPENSSL_LINUX)
 static ssize_t boringssl_getrandom(void *buf, size_t buf_len, unsigned flags) {
   ssize_t ret;
   do {
@@ -57,6 +60,7 @@ static ssize_t boringssl_getrandom(void *buf, size_t buf_len, unsigned flags) {
 
   return ret;
 }
+#endif  // OPENSSL_LINUX
 
 // kHaveGetrandom in |urandom_fd| signals that |getrandom| or |getentropy| is
 // available and should be used instead.
@@ -71,7 +75,8 @@ static CRYPTO_once_t rand_once = CRYPTO_ONCE_INIT;
 // requested. This is the only function that modifies |urandom_fd|, which may be
 // read safely after calling the once.
 static void init_once() {
-  int have_getrandom;
+  int have_getrandom = 0;
+#if defined(OPENSSL_LINUX)
   uint8_t dummy;
   ssize_t getrandom_ret =
       boringssl_getrandom(&dummy, sizeof(dummy), GRND_NONBLOCK);
@@ -88,6 +93,7 @@ static void init_once() {
     perror("getrandom");
     abort();
   }
+#endif  // OPENSSL_LINUX
 
   if (have_getrandom) {
     urandom_fd = kHaveGetrandom;
@@ -129,9 +135,12 @@ void bssl::CRYPTO_sysrand(uint8_t *out, size_t len) {
   while (len > 0) {
     ssize_t r;
 
+#if defined(OPENSSL_LINUX)
     if (urandom_fd == kHaveGetrandom) {
       r = boringssl_getrandom(out, len, 0);
-    } else {
+    } else
+#endif  // OPENSSL_LINUX
+    {
       do {
         r = read(urandom_fd, out, len);
       } while (r == -1 && errno == EINTR);
