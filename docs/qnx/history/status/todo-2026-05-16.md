# QNX Phase 2 — TODO

> Source-of-truth for remaining tasks.
> Updated: 2026-05-16

---

## Category Resolution Status

| Category | Description | Status | Fix Applied |
|----------|-------------|--------|-------------|
| 0 | toolchain `-I` conflict | 🟢 | args.gn restoration (environment issue) |
| A | Signal profiler tests excluded | 🟢 | `!is_qnx` guard added to `base/BUILD.gn` |
| B | Process priority API implementation | 🟢 | Added CanSetPriority/GetPriority/SetPriority to `process_qnx.cc` |
| C | Memory/Metrics implementation | 🟢 | Added UncheckedFree to `memory_qnx.cc`, GetMemoryInfo to `process_metrics_qnx.cc` |
| D | `GetCurrentEffectiveThreadTypeForTest` | 🟢 | QNX guard split in `platform_thread_posix.cc` |
| E | Perfetto TestTraceProcessor | 🟢 | `!is_qnx` guard added to `base/BUILD.gn` |
| F | Rust allocator shim link order | 🟢 | QNX link_command grouped into single link group in `gcc_toolchain.gni` |
| G | PartitionAlloc reentrancy crash | 🟢 | ThreadCache disabled, ScopedAllowAllocations added |
| H | ICU data file path (QNX FILE_MODULE) | 🟢 | CHROME_EXE_PATH environment variable fallback added |
| I | QNX epoll port introduced | 🟢 | `third_party/epoll/src` added as submodule, `libepoll` builds and links successfully |
| J | DeathTest exclusion (QNX fork() restriction) | 🟢 | Runtime exclusion via `--gtest_filter=-*DeathTest*` |
| K | spawn-based LaunchProcess implementation | 🟢 | `base/process/launch_qnx.cc` + BUILD.gn (`!is_qnx`) |

## Test Result Summary

### 2026-05-15 Test Run Results

| Item | Result |
|------|--------|
| Total tests | 7,891 |
| After DeathTest exclusion | ~1,423 FAILED; 1,300+ are IO-related (epoll missing) |
| FdWatchControllerPosixTest (before epoll fix) | 28/28 CRASHED (time=0.000) |
| FdWatchControllerPosixTest (after epoll fix, single-process) | **28/28 PASSED** ✅ |

### Known Issues

- **Batch mode crash**: `--single-process-tests` required. Test launcher spawn/fork conflicts with libepoll Resource Manager thread.
- **Problem resolved by epoll introduction**: MessagePumpEpoll PCHECK abort (~1,300 IO tests).

## Remaining Tasks

1. **GTEST_OS_QNX DeathTest support confirmation**: GTest has QNX spawn death-test implementation. Verify it is enabled in `build/config/qnx/BUILD.gn`.
2. Full test run (`--single-process-tests` + `--gtest_filter=-*DeathTest*`) — re-run with new LaunchProcess binary.
3. Batch mode issue investigation and fix (spawn/fork vs libepoll RM thread).
4. Build confirmation of other test targets (`content_unittests`, `browser_tests`, etc.).
5. Cleanup for upstream merge.
6. `--no-zygote --no-sandbox` runtime test.